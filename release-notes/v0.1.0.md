# Initial release of the supersecret provider.

## What's New

- Added `supersecret_hello` resource: creates a greeting from a given name.

## Usage

```hcl
resource "supersecret_hello" "example" {
  name = "world"
}
```
